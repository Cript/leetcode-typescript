import {expect} from "chai";
import {preorder} from "./01";

//TODO::add tests
describe('N-ary Tree Preorder Traversal', () => {
    // const cases: {
    //     s: string,
    //     expected: number
    // }[] = [
    //     {s: 'abccccdd', expected: 7},
    // ]
    //
    // describe('longestPalindrome', function() {
    //     cases.forEach(({s, expected}) => {
    //         describe(`When called with ${s}`, function() {
    //             it(`should return ${expected}`, function () {
    //                 expect(preorder(s)).equals(expected)
    //             })
    //         })
    //     })
    // })
})
