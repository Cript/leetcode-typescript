import {expect} from "chai";
import {newList} from "../common/linked-list";
import {detectCycle} from "./01";

//TODO::add tests
describe('Linked List Cycle II', () => {
    // const cases: {
    //     head: number[],
    //     expected: number
    // }[] = [
    //     {head: [3, 2, 0, -4], expected: 1},
    //     {head: [1, 2], expected: 0},
    // ]
    //
    // describe('middleNode', function() {
    //     cases.forEach(({head, expected}) => {
    //         describe(`When called with ${head}`, function() {
    //             it(`should return ${expected}`, function () {
    //                 const result = detectCycle(newList(head))
    //
    //                 console.log(result)
    //             })
    //         })
    //     })
    // })
})
